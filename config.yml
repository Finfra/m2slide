# m2slide Configuration
# Current project configuration

# Default project name (used by convert.sh and deploy.sh)
# Project path will be: Projects/{current_project}
current_project: LlmAndVibeCoding
# Optional: force top alignment of slide content
# When true, generated HTML adds a 'top-align-mode' class which overrides
# Reveal's vertical centering for the current slide. Use with care.
top_align: true
# Project-relative CSS for slide output (linked into each chapter HTML)
# Example: resource/slide.css
slide_css: resource/slide.css
